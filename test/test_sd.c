#include "../libraries/SD/SDCard.h"
#include <stdio.h>

#define PASS 1
#define FAIL 0

#define TRUE 1
#define FALSE 0

  /*
   * Unit tests for SD card
   */
   
  int main(void) {
	  
	  // a lot of my test scenarios are indeterminate (init, read ,write), because
	  // they depend upon parameters that change at run time. Can't test those, so
	  // instead test to ensure that your method is proper in writing 
	  
	  // Invalid temperature data
	  
	  // Invalid humidity data
	  
	  // Both Invalid data
	  
	  // All valid data
	  
	  
	  
	  
	  
	  
	  
  }